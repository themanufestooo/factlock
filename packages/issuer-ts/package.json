{
  "dependencies": {
    "@factlock/attestation-core": "file:../core-ts",
    "@factlock/keystore": "file:../keystore-ts",
    "@factlock/merkle-log": "file:../log-ts",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "description": "FactLock attestation issuance service \u2014 schema validation, server-stamped time, authorization gate, countersignature, log append (T3)",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "exports": {
    ".": {
      "default": "./dist/src/index.js",
      "types": "./dist/src/index.d.ts"
    }
  },
  "main": "dist/src/index.js",
  "name": "@factlock/issuer",
  "scripts": {
    "build": "rm -rf dist && tsc && cp -r schemas dist/schemas",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "type": "module",
  "types": "dist/src/index.d.ts",
  "version": "0.1.0"
}
