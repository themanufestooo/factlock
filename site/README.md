# FactLock landing page

The production-ready static site lives in `dist/`. It has no runtime dependencies and can be served by any static host.

The “unending FAQ” runs locally in the browser from a curated FactLock knowledge base. It intentionally declines questions it cannot answer reliably instead of fabricating product details.
